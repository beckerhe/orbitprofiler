--- a/src/3rdparty/chromium/third_party/angle/src/common/mathutil.cpp	2020-11-29 11:12:36.554313700 +0100
+++ b/src/3rdparty/chromium/third_party/angle/src/common/mathutil.cpp	2020-11-29 11:26:18.788442000 +0100
@@ -72,11 +73,11 @@
     const RGB9E5Data *inputData = reinterpret_cast<const RGB9E5Data *>(&input);
 
     *red =
-        inputData->R * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits);
+        static_cast<float>(inputData->R * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits));
     *green =
-        inputData->G * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits);
+        static_cast<float>(inputData->G * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits));
     *blue =
-        inputData->B * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits);
+        static_cast<float>(inputData->B * pow(2.0f, (int)inputData->E - g_sharedexp_bias - g_sharedexp_mantissabits));
 }
 
 int BitCountPolyfill(uint32_t bits)
